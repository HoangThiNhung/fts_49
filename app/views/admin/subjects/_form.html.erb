<%= form_for [:admin, @subject] do |f| %>
  <%= f.label :name  %>
  <%= f.text_field :name, class: "form-control" %>
  <br>
  <%= f.submit t("subjects.new.save"), class: "btn btn-primary" %>
<% end %>
